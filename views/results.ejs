<!DOCTYPE html>
<html>
  <head>
    <title>Search Results</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    
    <h1>Search Results</h1>
    <% if (results.length === 0) { %>
      <p>No results found.</p>
    <% } else { %>
      <% results.forEach(result => { %>
        <h2><%= result.name %></h2>
        <img src="<%= result.image %>" alt="">
        <p><strong>Name:</strong> <%= result.name %></p>
        <p><strong>Height:</strong> <%= result.height %></p>
        <p><strong>Home Planet:</strong> <%= result.homeworld %></p>
        <p><strong>Species:</strong> <%= result.species || 'Human' %></p>
        <p><strong>Starship:</strong> <%= result.starship %></p>
        <p><strong>Vehicle:</strong> <%= result.vehicle  %></p>
        <hr>
      <% }) %>
    <% } %>
    <a href="/">Back to Search</a>
  </body>
</html>